
describe('Scraping', () => {
    it('should scrap the requested data.', () => {
        const x = 2;
        const y = 4;

        const sum = x + y;

        //Esperado
        expect(sum).toBe(6)
    });
});
